# Прогоны агента с LLM — 23.09.2026

Коммит `2230aca6149c`, штатная policy `v4`, модель `gpt-5.6-luna`.
Python: `/home/midoriya/beeline/.venv/bin/python`; `AGENT_USE_LLM=1` во всех шести прогонах. **Примечание:** прогоны сделаны на `2230aca`, до появления флага — в том коде советника включал сам непустой `OPENAI_API_KEY`. С `a0e7b8b` (и в замороженном `f11ae2d`) советник включается только при `AGENT_USE_LLM=1` вместе с ключом. В контрольных процессах ключ удалён из окружения.

Оценка: `local_eval.evaluate_agent` на штатной `mock_environment`, с эффектами из CSV. Это локальный стенд, а не судейская модель; LLM-вызовы реальные. Net — `net_arpu_gain`: пилоты + финальные кампании после затрат на коммуникации, без стоимости API.

| Seed | Net с LLM | Net без ключа | Δ LLM − без ключа | Время с LLM / без ключа, с | Время API, с | Вызовы советника / HTTP-попытки |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | 3 131 603,43 | 3 262 701,94 | -131 098,51 | 10.437 / 0.855 | 9.689 | 1 / 2 |
| 1 | 3 224 904,72 | 3 431 580,68 | -206 675,96 | 15.860 / 0.807 | 15.087 | 1 / 2 |
| 2 | 3 371 733,03 | 3 417 068,95 | -45 335,93 | 9.433 / 0.746 | 8.689 | 1 / 2 |

Время измерено вокруг `evaluate_agent`: подготовка среды, `Agent.act` и скоринг; запуск Python и импорты исключены. Прогоны выполнялись последовательно в отдельных процессах. Без ключа — 0 вызовов и 0 HTTP-попыток.

Для каждого seed первый HTTP-запрос получил 400 из-за `temperature=0`; штатный повтор без этого параметра завершился успешно. Поэтому одна консультация означает две HTTP-попытки. API использовал температуру по умолчанию (1), повторный запуск LLM может дать другие решения.

**Перестановки arms:** для seeds 0, 1, 2 — ни одной. `ExplorerV4.run()` не вызывает `rank_confirmations`; этот этап подключён только в policy `v3`. В каждом прогоне выполнено 15 пилотов; в парах с/без ключа их логи и список finals до review полностью совпадают.

| Seed | Запрошено удалить (точные campaign_name) | Фактически удалено | Guardrail отказал? | Finals до → после |
|---:|---|---|---|---:|
| 0 | `tariff_8_HIGH_to_tariff_10_sms` | все перечисленные | нет | 8 → 7 |
| 1 | `tariff_10_MID_to_tariff_11_sms`; `tariff_11_LOW_to_tariff_12_push`; `tariff_8_LOW_to_tariff_10_push` | все перечисленные | нет | 9 → 6 |
| 2 | `tariff_8_LOW_to_tariff_10_push` | все перечисленные | нет | 9 → 8 |

Попыток удалить все финальные кампании или неизвестные имена не было. Советник объяснял удаления шумностью единичных малых пилотов и риском «проклятия победителя». В этих трёх прогонах фильтрация снизила net во всех seeds.

Исходные JSON с запросами/ответами, логами решений, пилотами, finals и скорингом, а также скрипт захвата: `/tmp/llm-runs/docs/llm_runs_20260923`. Ключи не сохранены. Отслеживаемые файлы не изменены.
